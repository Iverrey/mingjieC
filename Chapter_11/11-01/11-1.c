//将代码清单11-3中对p的赋值进行如下修改。
// p = "456" + 1;
//请编写程序确认运行结果，并对运行结果进行分析。
#include <stdio.h>

int main(void)
{
    char *p = "123";
    printf("p = \"%s\"\n", p);
    p = "456" + 1;             //将p指向"456"的第二个字符
    printf("p = \"%s\"\n", p); //输出从第二个字符5开始的字符串
    return 0;
}